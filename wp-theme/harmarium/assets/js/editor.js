/* Harmarium · editor enhancements (block-style class hints). */
(function (wp) {
  if (!wp || !wp.domReady) return;
  wp.domReady(function () {
    // No-op for now: block styles are registered in PHP. Hook reserved for future
    // "insert pattern" actions, format types, or rich-text controls.
  });
})(window.wp);
